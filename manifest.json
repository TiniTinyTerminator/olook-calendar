{
  "schemaVersion": 1,
  "id": "ttt.olook-calendar",
  "name": "Olook Calendar",
  "version": "1.0.4",
  "author": "TiniTinyTerminator",
  "license": "MIT",
  "description": "The clock and the calendar in one: date and time in the bar, a month marked with the days you have something on, the agenda behind it, and a notification before an appointment starts.",
  "kinds": [
    "bar-widget"
  ],
  "keepLoaded": true,
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "Olook Clock & Calendar",
    "description": "Date and time, a marked month, what is coming up, and a nudge before it does.",
    "category": "Time",
    "aliases": [
      "calendar",
      "clock",
      "agenda",
      "appointments",
      "olook-calendar"
    ],
    "allowMultiple": false,
    "defaultSection": "center",
    "defaults": {
      "refreshIntervalSec": 300,
      "showNextInBar": false,
      "daysAhead": 7,
      "format": "dddd HH:mm",
      "remindMinutes": 15
    },
    "schema": [
      {
        "key": "format",
        "type": "string",
        "label": "Label format",
        "defaultValue": "dddd HH:mm",
        "description": "Qt date format, as the Omarchy clock uses: dddd HH:mm, d MMMM yyyy, HH:mm."
      },
      {
        "key": "refreshIntervalSec",
        "type": "integer",
        "label": "Re-read the calendar every (seconds)",
        "min": 60,
        "max": 3600,
        "step": 60,
        "defaultValue": 300
      },
      {
        "key": "showNextInBar",
        "type": "boolean",
        "label": "Show the next appointment in the bar too",
        "defaultValue": false,
        "description": "Off by default: beside the clock, a bare time reads as a second clock."
      },
      {
        "key": "daysAhead",
        "type": "integer",
        "label": "Days of agenda in the panel",
        "min": 1,
        "max": 7,
        "step": 1,
        "defaultValue": 2
      },
      {
        "key": "remindMinutes",
        "type": "integer",
        "label": "Remind me this many minutes before",
        "min": 0,
        "max": 120,
        "step": 5,
        "defaultValue": 15,
        "description": "0 turns reminders off. All-day entries are never reminded about."
      }
    ]
  }
}
